{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "sprint_constraints.schema.json",
  "title": "SprintConstraints",
  "description": "Calendar and capacity constraints used for scheduling.",
  "type": "object",
  "required": ["planning_start_date", "working_days_per_week", "max_daily_utilization_percentage"],
  "additionalProperties": false,
  "properties": {
    "planning_start_date": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "ISO 8601 date — the earliest date any task may start"
    },
    "working_days_per_week": {
      "type": "integer",
      "minimum": 1,
      "maximum": 7,
      "description": "Number of working days per week (typically 5)"
    },
    "max_daily_utilization_percentage": {
      "type": "number",
      "minimum": 1,
      "maximum": 100,
      "description": "Maximum percentage of daily capacity that may be consumed (e.g. 85)"
    }
  }
}
