{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "EnvironmentState",
  "description": "Standard schema for an environment state record produced by Stage 05 (Config & environments)",
  "type": "object",
  "properties": {
    "component_id": {
      "type": "string",
      "description": "Unique identifier of the microservice/component"
    },
    "environment": {
      "type": "string",
      "enum": ["dev", "test", "staging", "prod"],
      "description": "Target deployment environment"
    },
    "deployed_version": {
      "type": "string",
      "description": "Version currently deployed in the target environment"
    },
    "expected_version": {
      "type": "string",
      "description": "Baseline version expected in this release window"
    },
    "drift_status": {
      "type": "string",
      "enum": ["in-sync", "drifted"],
      "description": "Indicates whether deployed version matches expected baseline"
    },
    "last_checked": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of last sync check"
    }
  },
  "required": [
    "component_id",
    "environment",
    "deployed_version",
    "expected_version",
    "drift_status",
    "last_checked"
  ]
}
